{
  "name": "brians-api",
  "version": "0.0.0",
  "description": "",
  "main": "bin/www",
  "scripts": {
    "dev": "nodemon --env-file=.env.local bin/www",
    "start": "node bin/www",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mig",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "mongoose": "^8.2.4"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
